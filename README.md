This project uses [uv](https://docs.astral.sh/uv/).

Run ``` uv sync --all-packages``` from root to install dependencies.

After that, run ``` uv run [path_to_file]``` to run python files.

If using ```pip```, just run ```pip install .```